// WIP
// Program to convert Decimal numbers to Binary, Octal, and Hexadecimal numbers

#include <stdio.h>

int main()
{
    int n, k, i, j;

    printf("Enter the decimal number: ");
    scanf("%d", &n);

    printf("Which base do you want to convert the decimal number into? ");
    scanf("%d", &k);

    int binary[10];

    while(1){
        n % k
        for(i = 0; i < 10; i++){
            binary[i] = 0
        }
    }

    return 0;
}