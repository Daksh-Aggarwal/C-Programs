#include <stdio.h>

int summation(int);

int main(){

    int n, sum = 0;
    printf("Enter the value of n: ");
    scanf("%d", &n);

    summation(n);

    return 0;
}

int summation(int n){
    
    if i > 0 return 
}
