// WIP

#include <stdio.h>

int main()
{
    int i, j, n;

    printf("Enter the number of rows: ");
    scanf("%d", &n);

    for(i = 1; i < 2*n; i++){
        if(i == 1 || i == (2*n) - 1){
            for(j = 1; j < 2*n; j++)
            printf("%d", n);
        }
        else{
            for(j = 1; j < )
        }
        printf("\n");
    }
}